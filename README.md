Chopin allows you to search a directory and count how many times each line appears accross all files in a given directory. 

Usage
```
$ git clone ...
$ cd chopin
$ node chopin.js chopin --dir ../path/to/dir --c 15
```